[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "reachy_mini_lib"
version = "0.1.0"
dependencies = ["reachy-mini[mujoco]"]   # official SDK on PyPI
